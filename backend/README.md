# Mindly Backend 🧠

Este é o servidor API do projeto Mindly, construído com Django e Django Rest Framework.

## Como rodar localmente
1. Instale o Poetry.
2. Execute `poetry install`.
3. Configure as variáveis de ambiente no arquivo `.env`.
4. Execute `poetry run python manage.py runserver`.

## Testes
Para rodar os testes:
`poetry run python manage.py test`